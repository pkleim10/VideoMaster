# User guide screenshots

These images are referenced from [../USER_GUIDE.md](../USER_GUIDE.md). Until you add real captures, the guide uses [placeholder.svg](placeholder.svg) so links stay valid.

## How to capture

1. Use **VideoMaster 0.6.6** or newer.
2. Prefer **window ~1200×800** (or your usual size) and **light or dark** mode — pick one and stay consistent across shots.
3. Hide unrelated apps; use a **clean desktop** or blur if you share publicly.
4. Save as **PNG** (good for UI) into this folder with the filenames below.
5. In `USER_GUIDE.md`, change each `images/placeholder.svg` to the matching `.png` when ready.

## Shot list

| Filename | What to show | How to get there |
|----------|----------------|------------------|
| `landing.png` | Welcome screen with create/open library | Quit library (or first launch) until you see the landing view |
| `main-window.png` | Full window: sidebar + grid + detail | Open a library, select a video, grid visible |
| `sidebar.png` | Sidebar only (crop or narrow window) | Same; show Library + Collections + Rating + Tags sections |
| `grid-view.png` | Grid of thumbnails | Toolbar: Grid segmented control |
| `list-view.png` | List/table with columns | Toolbar: List segmented control |
| `detail-pane.png` | Detail: thumbnail or filmstrip + metadata | One video selected; optional filmstrip tab |
| `detail-tags.png` | Tag chips in detail (optional) | Same; scroll to Tags if needed |
| `toolbar.png` | Top toolbar: Add Folder, Import, view mode, Surprise Me, sort | Crop top of browsing pane |
| `settings-library.png` | Settings → Library tab | **VideoMaster → Settings…** (library must be open) |
| `settings-video.png` | Settings → Video tab | Same |
| `settings-data-sources.png` | Settings → Data Sources with at least one folder | Same |
| `settings-file-ext.png` | Settings → File Ext | Same |
| `collection-editor.png` | New or edit collection sheet | Sidebar → **New Collection** |

## Optional extras

- **Missing filter**: sidebar **Missing** selected, **Scan for missing files** visible in toolbar.
- **Search**: search field with a query and results.
